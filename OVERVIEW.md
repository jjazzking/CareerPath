# CareerPath — 커리어 탐색 대시보드 Overview

## 1. 무엇을 만드는가

커리어에 대한 고민·탐색 주제를 **주제(Topic)별로 정리**하고, 각 주제 안에 내 생각·논리를 쌓아두면,
**주제별 AI**가 그 생각을 발전시키고 이전 맥락을 기억한 채로 **파일에 저장**해두는 개인 대시보드.

핵심 흐름:

```
주제 목록(대시보드)
   └─ 주제 클릭
        ├─ 내가 쌓아둔 생각/논리 (내가 쓴 원본)
        ├─ AI가 발전시킨 정리/제안 (AI 산출물)
        └─ [생각 추가] → AI가 기존 맥락 + 새 입력을 읽고 발전 → 파일로 저장
```

## 2. 데이터 모델 (파일 기반)

주제 하나 = 폴더 하나. "파일로 저장"이라는 요구를 그대로 살린다.

```
data/
  topics/
    <topic-slug>/
      meta.json          # 주제 제목, 생성/수정일, 태그, 상태(탐색중/보류/결론)
      thoughts.md        # 내가 직접 쓴 생각·논리 (append-only, 원본 보존)
      ai_memory.md       # AI가 발전시킨 누적 정리 = "기억". 매 턴 갱신
      history/
        2026-07-10.md    # 각 발전 턴의 스냅샷 (감사/롤백용)
```

- `thoughts.md` = 사람이 쓰는 곳(원본, 지우지 않음)
- `ai_memory.md` = AI가 매번 새로 갱신하는 "누적 기억". **다음 턴의 입력 컨텍스트가 이 파일**이다.
- `history/` = 각 턴 스냅샷. 나중에 "왜 이렇게 정리됐지" 추적 / 되돌리기.

> 핵심 설계 포인트: **AI는 스스로 기억하지 못한다.** "기억"은 `ai_memory.md`를 매 요청에
> 다시 읽어 넣어서 구현한다. 저장 = 기억. 그래서 파일 저장이 곧 메모리 메커니즘이다.

## 3. 화면 구성

| 화면 | 내용 |
|---|---|
| **Overview (대시보드)** | 주제 카드 그리드. 각 카드: 제목, 상태, 마지막 수정일, 요약 1~2줄 |
| **주제 상세** | 좌: 내 생각(`thoughts.md`) / 우: AI 정리(`ai_memory.md`) / 하단: 입력창 + [발전시키기] 버튼 |
| **히스토리** | 이 주제의 발전 턴 타임라인 (선택) |

## 4. AI가 하는 일 (한 턴)

버튼을 누르면 서버에서:

1. `ai_memory.md`(기존 기억) + `thoughts.md`의 새 내용 + 방금 입력한 텍스트를 읽는다
2. Claude API에 "이 주제에 대한 내 생각을 발전시키고, 빈틈/반론/다음 탐색 질문을 제시하고,
   갱신된 정리본을 만들어라"라고 요청한다
3. 응답을 `ai_memory.md`에 덮어쓰고, `history/<date>.md`에 스냅샷 저장
4. 대시보드 요약(meta.json)도 갱신

프롬프트가 매번 "이 주제 전용"으로 고정되므로 **prompt caching**으로 반복되는 시스템/기억
컨텍스트를 캐시해 비용·지연을 줄일 수 있다.

## 5. 기술 스택 (제안)

MVP 기준 가장 단순한 조합:

- **프론트+백**: Next.js (App Router) — 대시보드 UI + API Route에서 파일 IO
- **저장**: 로컬 파일시스템(`data/`) — DB 없이 시작. 나중에 필요하면 SQLite로 승격
- **AI**: Anthropic Claude API (`@anthropic-ai/sdk`), 모델 `claude-opus-4-8`
- **키 관리**: `.env`의 `ANTHROPIC_API_KEY` (프론트에 노출 금지, 서버에서만 호출)

> 대안: 대시보드 UI 없이 CLI/스크립트로 먼저 검증 → 파일 구조만 만들고 Claude 호출.
> 로직이 맞는지 확인한 뒤 UI를 얹는 것도 빠른 경로.

## 6. 구현 단계

1. **[스캐폴딩]** `data/topics/` 구조 + 주제 생성/목록 파일 IO
2. **[AI 한 턴]** 한 주제에 대해 `ai_memory.md`를 읽고→Claude 호출→갱신 저장하는 서버 함수
3. **[대시보드]** 주제 카드 그리드 + 상세 화면
4. **[다듬기]** 히스토리/롤백, 상태 태그, prompt caching, 요약 자동화

## 7. AI API를 여기 쓰는 게 맞나?  → 맞다. 단, 역할을 구분하라

**맞는 사용처다.** 이 프로젝트의 AI 역할은 전형적인 LLM 작업이다:

- 반쯤 정리된 생각을 **발전·구조화**하고
- **빈틈/반론/다음 질문**을 제시하고
- 누적 맥락을 반영해 **정리본을 다시 써주는** 것

이건 요약·확장·추론 계열이라 단발성 Claude API 호출(필요시 여러 턴 워크플로우)로 충분하다.
복잡한 에이전트/툴 루프는 초기엔 필요 없다.

**단, AI가 담당하지 않는 부분을 명확히:**

| 기능 | AI API 필요? |
|---|---|
| 주제 정리·저장·목록·화면 | ❌ 그냥 앱 로직 + 파일. AI 아님 |
| "기억한다" | ❌ API는 상태가 없다(stateless). 기억은 `ai_memory.md`를 매번 다시 넣어 구현 |
| 생각을 발전시키고 반론·질문 생성 | ✅ 여기가 AI API의 진짜 값어치 |

즉 "AI가 기억한다"는 착시고, 실제로는 **너의 파일 저장이 기억**이다. AI API는
"저장된 맥락을 읽고 생각을 밀어붙여 다시 써주는" 역할만 한다. 이 경계를 지키면
비용도 예측 가능하고(발전 버튼 누를 때만 호출), 구조도 단순해진다.

> 정리: **저장·조직화 = 앱 / 발전·추론 = AI API.** 이 프로젝트엔 AI API가 적절하다.
